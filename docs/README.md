# **Проект интернет-магазином.**

## Описание:



## Нововведение и функции:
###  Нововведение для пользователей1. Была добавлена функция 

###  Нововведение технические не предназначенные для глаз пользователя
1. Была введена функция Класс для представления продукта.
2. Была введена функция Класс для представления категории товаров
3. Была введена функция чтение json файла 
 
Дальнейшие функции будут описаны в последующих обновлениях.

### **Примечание к функциям**


## Инструкция по настройке линтеров для данного проекта

Создайте файл с названием ".flake8" и в нём введите следующие
```
[flake8]
max-line-length = 119
ignore = E402, E251, E203
exclude = .git, .venv, __pycache__, venv

```
Вводите в файл pyproject.toml, если там не прописано, то пропишите следующие.
```
[tool.black]
# Максимальная длина строки
line-length = 119
# Файлы, которые не нужно форматировать
exclude = '''
(
  /(
      \.eggs         # Исключить несколько общих каталогов
    | \.git          # в корне проекта
    | \.hg
    | \.mypy_cache
    | \.tox
    | \.venv
    | dist
  )/
  | foo.py           # Также отдельно исключить файл с именем foo.py
                     # в корне проекта
)
'''


[tool.isort]
# максимальная длина строки
line_length = 119


[tool.mypy]
disallow_untyped_defs = true
warn_return_any = true
ignore_missing_imports = true
exclude = "venv"

```
И все зависимости что есть на данный момент будут прописаны.

## **Тестирование кодов**

#### ТЕСТЫ ДЛЯ КЛАССА Product

1. Проверка корректности инициализации Product через параметризацию данных
2. Проверка атрибутов продукта, созданного через фикстуру

#### ТЕСТЫ ДЛЯ КЛАССА Category

1. Проверка корректности инициализации Category через параметризацию данных
2. Проверка атрибутов категории, созданной через фикстуры

#### ТЕСТЫ ДЛЯ АТРИБУТОВ КЛАССА (СЧЕТЧИКИ)

1. Проверка автоматического подсчета категорий и товаров через параметризацию
2. Проверка счетчиков с использованием фикстур
